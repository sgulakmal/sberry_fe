// app/feed/Feed.tsx
'use client';
// import { RootState } from '@/lib/store';
// import { useSelector } from 'react-redux';


export default function Feed() {
  // const posts = useSelector((state: RootState) => state.feed.posts);

  return (
    <div>
      {/* {posts.map((post) => (
        <div
          key={post.id}
          className="border-b py-4 px-2 bg-white rounded shadow mb-3"
        >
          <div className="font-semibold text-sm text-gray-700">{post.user}</div>
          <div className="text-gray-900 mt-1">{post.content}</div>
          <div className="text-xs text-gray-500 mt-2">{new Date(post.createdAt).toLocaleString()}</div>
        </div>
      ))} */}
    </div>
  );
}
